import SwiftUI

/// The approved Ember Lens vector, generated from the shared brand master.
struct CampLogo: View {
    var body: some View {
        Image("BrandMark")
            .resizable()
            .scaledToFit()
            .accessibilityLabel("AI Film Camp")
    }
}
