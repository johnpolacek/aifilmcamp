import Foundation

enum ExtractionDisclosureText {
    static let firstRun = """
    Analyzing sends the full text of this screenplay to Codex through your own Codex account. Codex may include your global Codex instructions and the descriptions of your installed Codex skills or plugins in the same request; AI Film Camp does not read or store those. AI Film Camp keeps copies of the screenplay chunks it sent inside this project (cache/jobs) so an interrupted run can resume — you can clear them from the Jobs section. Nothing is sent until you choose Continue.
    """
}
